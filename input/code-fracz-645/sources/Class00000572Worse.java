// original filename: 00060559.txt
// after
public class Class00000572Worse {
public int showDialog() {
    // if in- nor outgoing checkbox is set, check both
    if (wsUiHelper.getDirection() == null) {
        wsUiHelper.setDirection(null);
    }
    setVisible(true);
    return exitResult;
}
}