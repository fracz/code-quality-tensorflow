// original filename: 00001032.txt
// before
public class Class00000566Better {
public void addWidget(DashboardWidget widget) throws ValidationException {
    embed(EMBEDDED_WIDGETS, widget);
    widgets.put(widget.getId(), widget);
}
}