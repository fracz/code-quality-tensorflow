// original filename: 00053579.txt
// after
public class Class00000166Better {
private Token scanChars() {
    Tokenizer tokenizer = new Tokenizer(input, dictionary, position);
    tokenizer.scanChars();
    return new Token(tokenizer);
}
}