// original filename: 00053579.txt
// before
public class Class00000166Worse {
protected void scanChars() {
    Tokenizer tokenizer = new Tokenizer(input, dictionary, position);
    tokenizer.scanChars();
    setTokenizerResult(tokenizer);
}
}