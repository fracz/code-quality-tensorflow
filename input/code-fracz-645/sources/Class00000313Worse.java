// original filename: 00003246.txt
// before
public class Class00000313Worse {
public boolean process(Annotator annotator) {
    if (dumb && !(annotator instanceof DumbAware)) {
        return true;
    }
    annotator.annotate(element, annotationHolder);
    return true;
}
}